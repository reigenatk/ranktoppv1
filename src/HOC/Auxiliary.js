// dont even need to import react here...
// only purpose of this HOC is to act as a way to render multiple JSX components together in other
// components

const aux = (props) => props.children;

export default aux;
